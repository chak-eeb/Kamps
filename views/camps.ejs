<%- include ('partials/header') %>

<h1>this is the camps route</h1>

<% camps.forEach(function(camp){ %>
<div>
    <h4> <%= camp.name %><h4>
        <img src="<%= camp.image %>">
</div>
<% }); %>

<%- include ('partials/footer') %>
